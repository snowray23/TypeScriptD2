// Task 1: Define a TypeScript interface named UserInfoProps with properties for user name, age, and email.

interface UserInfoProps {
  name: string;
  age: number;
  email: string;
}

export default UserInfoProps;